"use server";

// Version 0: static UI — no database operations
export async function postComment(_taskId: string, _text: string) {
  // no-op
}

export async function uploadAttachment(_taskId: string, _formData: FormData) {
  // no-op
}
